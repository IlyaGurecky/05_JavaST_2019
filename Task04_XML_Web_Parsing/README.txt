This web application allows you to parse XML document by three parse models(DOM, SAX, StAX).

You can upload xml file with tariffs into server and parse them into table.

All pages are localized(EN, BEL, RUS).

XML file validates by XSD schema.
