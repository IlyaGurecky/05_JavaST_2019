This program lets to user calculate square and perimeter of quadrangle and
determine its type. You can add figure to the repository and if the figure has
been changed its characteristics are recalculated.
The same way, you can get and sort the figures according to
a different specification.

Input data in file: 4 2D dots (8 numbers). The points should be entered
clockwise or counterclockwise.

To calculate parameters program uses algorithms based on geometrical
mathematical formulas(Gauss Square Formula for example).

Code written with plugins "CheckStyle" and "SonarLint".

Using technologies: TestNG, Log4j2, Java stream API.

Using Patterns: Observer, Singleton, Factory method.

                        Variant 2.
